@model MBlog.Models.PostsViewModel
@{
    ViewBag.Title = "Blog Posts";
    Layout = "~/Views/Shared/_BlogLayout.cshtml";
}
<ul class="blog-list">
    @foreach (var blogViewModel in Model.Posts)
    {
        <li class="blog-post">
            @{
                Html.RenderPartial("ShowSinglePost", blogViewModel);
            }
        </li>
    }
</ul>
